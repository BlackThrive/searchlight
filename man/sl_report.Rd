% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/report.R
\name{sl_report}
\alias{sl_report}
\title{Write an offline report of audited records and analysis results}
\usage{
sl_report(
  records,
  file,
  estimates = list(),
  title = "Stop and search: evidence and assumptions",
  assumptions = character(),
  limitations = character(),
  max_rows = 500,
  overwrite = FALSE
)
}
\arguments{
\item{records}{Contract-bearing search records.}

\item{file}{Explicit HTML output path. Its parent directory must exist.}

\item{estimates}{Uniquely named list of contract-bearing result tables.}

\item{title}{Report title, treated as text.}

\item{assumptions, limitations}{Additional statements, treated as text.}

\item{max_rows}{Maximum displayed rows per table; truncation is labelled.}

\item{overwrite}{Whether to replace an existing output file.}
}
\value{
Invisibly, a one-row tibble with the output path and record count,
carrying the ingestion contract. Writes only the requested output file.
}
\description{
Renders the installed Markdown/Rmd template to a self-contained HTML file.
No Pandoc, network, model fitting or external web assets are required.
Supplied estimates must retain the same source snapshots and ethnicity
classification as records. Their analysis scope and diagnostics are shown.
Coverage describes source files; filtering does not redefine coverage.
Sampling intervals and assumption ranges have separate labelled sections.
The report includes a summary, section navigation and expandable audit
details. Tables retain their original column names in header tooltips.
}
\examples{
rates <- readRDS(system.file("extdata", "example-rates.rds",
  package = "searchlight"
))
path <- tempfile(fileext = ".html")
sl_report(sl_sample(), path, estimates = list(ratios = sl_rate_ratio(rates)))
file.exists(path)
}
\seealso{
\code{\link[=sl_coverage]{sl_coverage()}}, \code{\link[=sl_rate_ratio]{sl_rate_ratio()}}, \code{\link[=sl_missing_ethnicity_bounds]{sl_missing_ethnicity_bounds()}}
}
\concept{reporting}
