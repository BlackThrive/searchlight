% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rates-baseline.R
\name{sl_counts}
\alias{sl_counts}
\title{Count events while preserving submission strata}
\usage{
sl_counts(
  records,
  by = c("pfa", "month", "ethnicity_5"),
  units = NULL,
  months = NULL,
  forces = NULL
)
}
\arguments{
\item{records}{Contract-bearing records.}

\item{by}{Additional grouping columns, including exactly one ethnicity field.}

\item{units}{Optional force_id and geography mapping defining the full area
universe, including areas without events. Otherwise uses contract units or
observed areas. PFA defaults to the published force-code mapping.}

\item{months, forces}{Explicit analysis scope; defaults to contract coverage.}
}
\value{
An sl_counts tibble with canonical geography_code, ethnicity, n,
force_id and month columns, original grouping fields, and coverage status.
}
\description{
Force and month always remain in the result, even if omitted from by: these
identify independent reporting and time-exposure cells. Unknown ethnicity
remains a level. Missing submissions have NA counts; submitted combinations
without events have zero. Filtering records does not narrow the source time
grid: pass months and forces explicitly to select the analysis period.
}
\examples{
counts <- sl_counts(sl_sample())
head(counts)
}
\seealso{
\code{\link[=sl_rates]{sl_rates()}}, \code{\link[=sl_coverage]{sl_coverage()}}

Other rates: 
\code{\link{sl_rate_ratio}()},
\code{\link{sl_rates}()}
}
\concept{rates}
