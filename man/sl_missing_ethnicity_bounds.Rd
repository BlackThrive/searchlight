% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sens-missing-ethnicity.R
\name{sl_missing_ethnicity_bounds}
\alias{sl_missing_ethnicity_bounds}
\title{Bound disparity under allocations of Unknown ethnicity}
\usage{
sl_missing_ethnicity_bounds(
  counts,
  population,
  reference = "White",
  comparison = "Black",
  scenarios = c("all_to_reference", "all_to_comparison", "proportional",
    "force_object_mar")
)
}
\arguments{
\item{counts}{Contract-bearing event counts.}

\item{population}{Marginal ethnic-group exposure table.}

\item{reference, comparison}{Groups to compare.}

\item{scenarios}{Allocation assumptions to report.}
}
\value{
An sl_sensitivity tibble with scenario ratios, extreme bounds,
tipping allocation and separately labelled baseline sampling intervals.
}
\description{
Extreme allocations send every Unknown event to one of the two groups.
Proportional allocation uses all known groups, not only the comparison pair.
force_object_mar uses known-group shares within force and object, pooled over
areas and months; it refuses strata with no known ethnicity. These are
assumptions, not confidence intervals. The tipping share solves the equation
where the ratio equals one, with all remaining Unknown sent to the reference.
A tipping value outside zero to one means no feasible tipping allocation.
}
\examples{
c <- readRDS(system.file("extdata", "example-counts.rds",
  package = "searchlight"
))
p <- readRDS(system.file("extdata", "sample-population.rds",
  package = "searchlight"
))$msoa21
head(sl_missing_ethnicity_bounds(c, p))
}
\seealso{
\code{\link[=sl_rates]{sl_rates()}}, \code{\link[=sl_denominator_scenarios]{sl_denominator_scenarios()}}

Other sensitivity: 
\code{\link{sl_denominator_scenarios}()},
\code{\link{sl_ranking_stability}()},
\code{\link{sl_standardise}()}
}
\concept{sensitivity}
