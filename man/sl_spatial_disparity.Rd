% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/infer-spatial.R
\name{sl_spatial_disparity}
\alias{sl_spatial_disparity}
\title{Estimate a multivariate spatial stop-rate disparity surface}
\usage{
sl_spatial_disparity(
  counts,
  population,
  boundaries,
  reference = "White",
  comparison = "Black",
  backend = c("carbayes", "inla"),
  burnin = 2000,
  n.sample = 12000,
  thin = 10,
  chains = 2,
  k = 2,
  adjacency = NULL,
  boundary_threshold = 0.2,
  seed = 1,
  ...
)
}
\arguments{
\item{counts}{Marginal event counts with a valid contract.}

\item{population}{Compatible marginal ethnicity exposure table.}

\item{boundaries}{sf polygons with geography_code.}

\item{reference, comparison}{Known self-defined ethnicity groups.}

\item{backend}{CARBayes. INLA is explicitly deferred beyond version 0.1.0.}

\item{burnin, n.sample, thin}{MCMC controls per chain, including burn-in
in n.sample.}

\item{chains}{Number of independent chains, at least two, run on one core.}

\item{k}{Additional ratio threshold for exceedance probability.}

\item{adjacency}{Optional symmetric adjacency matrix named with
geography codes.
Otherwise rook adjacency is used; islands require an explicit decision.}

\item{boundary_threshold}{Warn if the assigned boundary-sensitive share
exceeds
this proportion (default 0.2).}

\item{seed}{Reproducible seed, restored on exit.}

\item{...}{Prior and sampler arguments for MVS.CARleroux, such as rho
or MALA.}
}
\value{
An sl_spatial tibble with 90/95 percent intervals, exceedance
probabilities, crude ratios, rank-normalised R-hat and bulk/tail ESS.
Joint
posterior draws, fitted model, diagnostics and boundaries are attributes.
}
\description{
Uses CARBayes MVS.CARleroux with two Poisson responses and a matrix of log
population-time offsets. Each ethnicity has its own spatial effect phi_ig.
The equivalent shared/contrast decomposition is u_i=(phi_ir+phi_ic)/2,
v_ir=(phi_ir-phi_ic)/2 and v_ic=-v_ir. These components have the covariance
induced by the multivariate prior; they are not separately independent
priors.
The full posterior ratio is exp(alpha_c-alpha_r+phi_ic-phi_ir).
Smoothing stabilises under stated assumptions; it cannot repair exposure,
missing ethnicity, missing submissions or anonymised location errors.
}
\examples{
# A precomputed synthetic fit avoids MCMC in installed examples.
path <- system.file("extdata", "example-spatial.rds",
  package = "searchlight"
)
if (nzchar(path)) {
  fit <- readRDS(path)
  head(fit)
  head(sl_spatial_map(fit))
}
}
\seealso{
\code{\link[=sl_simulate]{sl_simulate()}}, \code{\link[=sl_spatial_map]{sl_spatial_map()}}, \code{\link[=sl_ranking_stability]{sl_ranking_stability()}}

Other inference:
\code{\link{sl_count_model}()},
\code{\link{sl_hit_rates}()},
\code{\link{sl_simulate}()},
\code{\link{sl_spatial_map}()},
\code{\link{sl_veil_of_darkness}()}
}
\concept{inference}
