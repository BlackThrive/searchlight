% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sens-standardise.R
\name{sl_standardise}
\alias{sl_standardise}
\title{Directly standardise age-sex stop-event rates}
\usage{
sl_standardise(
  counts,
  crosstab,
  standard = c("england_wales", "study_population"),
  per = 1000
)
}
\arguments{
\item{counts}{Counts grouped by age_band and sex.}

\item{crosstab}{Compatible Census or explicit ethnicity-age-sex exposure.}

\item{standard}{England/Wales Census 2021 or pooled study population weights.}

\item{per}{Rate scaling, default 1000 per person-year.}
}
\value{
An sl_sensitivity tibble with crude and standardised rates, sampling
intervals, excluded-event counts, weights and the ingestion contract.
}
\description{
Uses common age-sex weights across ethnicity groups. Census sex is compared
with police-recorded gender only under an explicit measurement assumption.
Unknown age, gender and ethnicity are retained in exclusion diagnostics.
A positively weighted stratum with zero exposure makes the standardised rate
undefined; it is never silently dropped. Sampling intervals combine exact
Poisson stratum intervals with a Bonferroni correction and fixed weights.
They are conservative, including when every observed stratum count is zero.
}
\examples{
p <- readRDS(system.file("extdata", "sample-crosstab.rds",
  package = "searchlight"
))
c <- readRDS(system.file("extdata", "example-demographic-counts.rds",
  package = "searchlight"
))
head(suppressWarnings(sl_standardise(c, p)))
}
\seealso{
\code{\link[=sl_population_crosstab]{sl_population_crosstab()}}, \code{\link[=sl_rates]{sl_rates()}}

Other sensitivity: 
\code{\link{sl_denominator_scenarios}()},
\code{\link{sl_missing_ethnicity_bounds}()},
\code{\link{sl_ranking_stability}()}
}
\concept{sensitivity}
