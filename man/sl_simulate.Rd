% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate.R
\name{sl_simulate}
\alias{sl_simulate}
\title{Simulate event counts with independent disparity and total intensity}
\usage{
sl_simulate(
  side = 6,
  surface = c("smooth", "discontinuous", "small"),
  population = c(White = 5000, Black = 1000),
  rate = 0.025,
  missingness = c("none", "mcar", "mar", "mnar"),
  missing_rate = 0.2,
  months = 12,
  seed = 1
)
}
\arguments{
\item{side}{Lattice width and height, at least three.}

\item{surface}{Smooth, discontinuous, or small comparison-population
setting.}

\item{population}{Resident reference/comparison populations per area.}

\item{rate}{Mean total annual event rate per resident.}

\item{missingness}{Missing-ethnicity mechanism.}

\item{missing_rate}{Baseline missing probability.}

\item{months}{Number of submitted months, between one and twelve.}

\item{seed}{Reproducible seed, restored on exit.}
}
\value{
A list of counts, complete_counts, population, boundaries,
truth and
settings. Counts carry a clearly synthetic ingestion contract.
}
\description{
The disparity surface varies east-west and expected total events
north-south.
Their lattice correlation is zero by construction. Counts are Poisson
events,
then unknown ethnicity is generated by binomial thinning. MCAR has a common
probability, MAR varies by synthetic force, and MNAR varies by ethnicity.
True latent rate ratios and complete realised event ratios are distinct.
}
\examples{
s <- sl_simulate(side = 3, missingness = "mnar", missing_rate = 0.3)
s$truth
sl_missing_ethnicity_bounds(s$counts, s$population,
  scenarios = c("all_to_reference", "all_to_comparison")
)
}
\seealso{
\code{\link[=sl_spatial_disparity]{sl_spatial_disparity()}}, \code{\link[=sl_missing_ethnicity_bounds]{sl_missing_ethnicity_bounds()}}

Other inference:
\code{\link{sl_count_model}()},
\code{\link{sl_hit_rates}()},
\code{\link{sl_spatial_disparity}()},
\code{\link{sl_spatial_map}()},
\code{\link{sl_veil_of_darkness}()}
}
\concept{inference}
