% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/infer-count-models.R
\name{sl_count_model}
\alias{sl_count_model}
\title{Fit count models with population-time exposure}
\usage{
sl_count_model(
  counts,
  formula,
  family = c("poisson", "negbin"),
  offset = "population",
  random = NULL,
  boundaries = NULL,
  conf_level = 0.95
)
}
\arguments{
\item{counts}{Counts with a contract and exposure column, usually sl_rates.}

\item{formula}{A formula with response n and desired fixed predictors.}

\item{family}{Poisson or negative binomial.}

\item{offset}{Name of the population or population-time exposure column.}

\item{random}{Optional one-sided lme4 formula, for example ~ (1 |
force_id).}

\item{boundaries}{Optional sf polygons keyed by geography_code for
residuals.}

\item{conf_level}{Wald confidence level.}
}
\value{
A tidy sl_count_model coefficient table with model, diagnostics and
excluded row/event counts in attributes, carrying the ingestion contract.
}
\description{
Models recorded events, never unique people or a population-minus-stops
cell.
A population offset is multiplied by months_submitted/12 when available.
Other offset columns must already represent exposure over the observed
period.
Unknown ethnicity and rows without positive exposure are reported as
exclusions.
Moran's I uses mean Pearson residuals per area and is exploratory after
fitting.
The exponentiated intercept is a baseline rate per exposure unit; other
exponentiated coefficients are multiplicative rate effects. Confidence
bounds are reported on this exponentiated scale.
}
\examples{
r <- readRDS(system.file("extdata", "example-rates.rds",
  package = "searchlight"
))
r <- r[r$ethnicity \%in\% c("White", "Black"), ]
sl_count_model(r, n ~ ethnicity)
# Supply matching boundaries to request the residual Moran diagnostic.
}
\seealso{
\code{\link[=sl_rates]{sl_rates()}}, \code{\link[=sl_spatial_disparity]{sl_spatial_disparity()}}

Other inference:
\code{\link{sl_hit_rates}()},
\code{\link{sl_simulate}()},
\code{\link{sl_spatial_disparity}()},
\code{\link{sl_spatial_map}()},
\code{\link{sl_veil_of_darkness}()}
}
\concept{inference}
