## Development status

Not a submission. Check results are appended as milestones are verified.

## Test environments

Local: Windows, R 4.5.2. Remote matrix, rhub and win-builder not yet run.

M0 and M1 local `--as-cran --no-manual` checks: 0 errors, 0 warnings, 0 notes.
Remote incoming checks and the network clock probe were disabled for these
offline runs. Final M1 examples all ran in under five seconds. Line coverage
measured during M1: 92.2619%. Logs are in `inst/validation/`.

## Reverse dependencies

None (new package).
